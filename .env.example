# Copy this file to `.env` and fill real values. Do NOT commit `.env`.

# Backend
PORT=3000
NODE_ENV=production
MONGODB_URI=mongodb://mongo:27017/conduiteprojet
# JWT_SECRET should be provided securely in production (see README or use Docker secrets)
JWT_SECRET=change_me_replace_in_production
JWT_EXPIRES_IN=1d

# Frontend
# FRONTEND_ORIGIN can be set to limit allowed origins in CORS
# FRONTEND_ORIGIN=http://example.com
# Example environment file
# Copy to .env and edit values before running docker compose

# Backend
PORT=3000
NODE_ENV=production
MONGODB_URI=mongodb://mongo:27017/conduiteprojet
JWT_SECRET=change_me
JWT_EXPIRES_IN=1d

# Frontend
# FRONTEND is served on port 80 by default in docker-compose
